{
  "name": "template-two",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "sass scss/main.scss:css/main.min.css --style=compressed --source-map",
    "watch": "sass scss/main.scss:css/main.css --watch",
    "dev": "sass scss/main.scss:css/main.css --watch",
    "build:dev": "sass scss/main.scss:css/main.css",
    "build:prod": "sass scss/main.scss:css/main.min.css --style=compressed --source-map",
    "clean": "rm -rf css/*.css css/*.map",
    "serve": "npx live-server --port=3000 --open=/"
  },
  "private": true
}

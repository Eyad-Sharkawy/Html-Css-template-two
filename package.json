{
  "name": "template-two",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx sass scss/main.scss:css/main.min.css --style=compressed --source-map",
    "watch": "npx sass scss/main.scss:css/main.css --watch",
    "dev": "npx sass scss/main.scss:css/main.css --watch",
    "build:dev": "npx sass scss/main.scss:css/main.css",
    "build:prod": "npx sass scss/main.scss:css/main.min.css --style=compressed --source-map",
    "clean": "rm -rf css/*.css css/*.map",
    "serve": "npx live-server --port=3000 --open=/"
  },
  "dependencies": {
    "sass": "^1.69.0"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  },
  "private": true
}
